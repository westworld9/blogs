<div class="container">
  <%= form_with(model: @blog, local: true) do |f| %>  
    <div class="blog-title">
      <%= f.label :title %> 
      <%= f.text_field :title %> 
    </div> 
    <div class="blog-title">
      <%= f.label :content %> 
      <%= f.text_field :content %>
    </div>
      <%= f.submit "投稿",class:"btn" %> 
  <% end %>
</div>